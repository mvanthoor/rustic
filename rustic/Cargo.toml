[package]
name = "rustic"
version.workspace = true
authors.workspace = true
edition.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
librustic = { path = "../librustic" }
clap = "4.5.40"

[features]
extra = []
